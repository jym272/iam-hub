name: team-budget-control
runtime:
  name: nodejs
  options:
    typescript: true
    packagemanager: bun
description: Team IAM management with individual budget controls and enforcement
main: ./src/infra.ts

config:
  aws:region: sa-east-1

# Optional: Configure default stack settings
template:
  config:
    aws:region:
      description: The AWS region to deploy resources
      default: sa-east-1
